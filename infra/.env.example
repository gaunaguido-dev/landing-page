# Used for local CDK operations only.
# In CI/CD these are injected automatically by the GitHub Actions workflow.

AWS_PROFILE=default
AWS_REGION=us-east-1

# Override email defaults (also settable via --context in cdk.json)
# CDK_FROM_EMAIL=hola@g2techwork.com
# CDK_TO_EMAIL=hola@g2techwork.com
