# Used for local CDK operations only.
# In CI/CD these are injected automatically by the GitHub Actions workflow.

AWS_PROFILE=default
AWS_REGION=us-east-1

# Recipient for contact-form notifications (defaults to contact@g2techwork.com)
TO_EMAIL=contact@g2techwork.com
