{
  "name": "g2techwork-infra",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build":   "tsc --noEmit",
    "synth":   "cdk synth",
    "diff":    "cdk diff --all",
    "deploy":  "cdk deploy --all --require-approval never",
    "destroy": "cdk destroy --all"
  },
  "dependencies": {
    "aws-cdk-lib": "^2.160.0",
    "constructs":  "^10.4.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "aws-cdk":     "^2.160.0",
    "esbuild":     "^0.24.0",
    "ts-node":     "^10.9.0",
    "typescript":  "^5.6.0"
  }
}
