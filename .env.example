# Public env vars embedded into the Astro static build.
# Copy to .env and fill in the value after running `terraform apply`.
#
# Get this value with: cd infra/terraform && terraform output api_endpoint

PUBLIC_API_URL=https://xxxx.execute-api.us-east-1.amazonaws.com/contact
